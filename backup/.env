# Arquivo .env para script de backup
# Substitua os valores abaixo pelos reais antes de executar o script.

# Conexão IMAP
IMAP_HOST=
IMAP_PORT=993
USE_SSL=true

# Diretórios
BACKUP_DIR=backups
LOG_DIR=logs

# CSV com contas (relativo ao diretório onde o script roda)
BACKUP_CSV=backup.csv
